import React from 'react';
import {
    View,
    Button,
    Text,
    TextInput,
    FlatList,
    TouchableOpacity,
    StyleSheet,
} from 'react-native';

interface Props {}

export const RegisterScreen: React.FC<Props> = ({}) => {
    return <View></View>;
};
