import React from 'react';
import {
    View,
    Button,
    Text,
    TextInput,
    FlatList,
    TouchableOpacity,
    StyleSheet,
} from 'react-native';

interface Props {}

const AuthScreen: React.FC<Props> = ({}) => {
    return <View></View>;
};

export default AuthScreen;
