import React from "react";
import {
  View,
  Button,
  Text,
  TextInput,
  FlatList,
  TouchableOpacity,
  StyleSheet,
} from "react-native";

interface Props {}

export const ProfileScreen: React.FC<Props> = ({}) => {
  return (
    <View>
      <Text>ProfileScreen</Text>
    </View>
  );
};
