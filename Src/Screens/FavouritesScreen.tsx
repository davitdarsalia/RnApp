import React from "react";
import {
  View,
  Button,
  Text,
  TextInput,
  FlatList,
  TouchableOpacity,
  StyleSheet,
} from "react-native";

interface Props {}

export const FavouritesScreen: React.FC<Props> = ({}) => {
  return (
    <View>
      <Text>Favourites Screen</Text>
    </View>
  );
};
