import React from 'react';
import {
   View,
   Button,
   Text,
   TextInput,
   FlatList,
   TouchableOpacity,
   StyleSheet,
} from 'react-native';

interface Props {}

export const MyLoans: React.FC<Props> = ({}) => {
   return (
      <View>
         <Text>Loans</Text>
      </View>
   );
};
