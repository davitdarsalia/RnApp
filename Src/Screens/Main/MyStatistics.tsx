import React from 'react';
import {
   View,
   Button,
   Text,
   TextInput,
   FlatList,
   TouchableOpacity,
   StyleSheet,
} from 'react-native';

interface Props {}

export const MyStatistics: React.FC<Props> = ({}) => {
   return (
      <View>
         <Text>Statistics</Text>
      </View>
   );
};
