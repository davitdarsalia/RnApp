import {StyleSheet} from 'react-native';

const st = StyleSheet.create({
    container: {
        alignItems: 'center',
        paddingVertical: 15,
        paddingHorizontal: 25,
    },
});

export default st;
