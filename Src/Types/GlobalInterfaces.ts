export interface registration {
    registration: string;
    password: string;
}

export interface registrationType {
    fullName: string;
    email: string;
    password: string;
    checked: string;
}

export interface defCoords {
    latitude: number;
    longitude: number;
}
